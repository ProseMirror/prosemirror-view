ProseMirror's _view_ displays a given editor state in the DOM, and
handles user events.

Make sure you load `style/prosemirror.css` as a stylesheet if you want
to instantiate a ProseMirror editor.

@EditorView

@EditorProps

@NodeView

@Decoration

@DecorationAttrs

@DecorationSet
